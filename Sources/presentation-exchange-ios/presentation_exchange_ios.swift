import Foundation

public struct presentation_exchange_ios {
    public private(set) var text = "Hello, World!"

    public init() {
      
//      // Get URL of config.json
//      let configURL = Bundle.module.url(forResource: "input-descriptor", withExtension: "json")
//      
//      print(configURL)
    }
}
